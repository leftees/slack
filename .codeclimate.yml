engines:
    fixme:
        enabled: true
    gofmt:
        enabled: true
    golint:
        enabled: true
    govet:
        enabled: true

ratings:
    paths:
    - "**/*(?!test).go"

exclude_paths:
- "**/*_test.go"
